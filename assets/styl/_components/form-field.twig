<form action="#">
    <div class="form-field">
        <p>
            <label for="{{ modifier_class|default('normal')|replace({'.': ''}) }}">Label</label>
            <input class="{{ modifier_class }}" type="text" id="{{ modifier_class|default('normal')|replace({'.': ''}) }}" />
        </p>
    </div>
</form>
