// Box
//
// A 'box' is a content container.
// It can be displayed as a block (on homepage or listing pages) or as a page (content view). It can also come with a footer.
//
// Markup: box.twig
//
// Styleguide Helpers.Box
.box
    position relative
    padding 1px
    border 1px solid $color-borders
    background-color $color-white

    &-footer
        {fontsecondary}
        {clearfix}
        margin 0
        padding-top .6rem
        padding-bottom .3rem
        background-color $color-bg-base
