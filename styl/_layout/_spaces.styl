/* margins and paddings */
$gutter = 12 // gutter width
for $i in (0..6)
    .pa{$i}
        padding $gutter * $i * 0.1rem

    .pt{$i}
        padding-top $gutter * $i * 0.1rem

    .pr{$i}
        padding-right $gutter * $i * 0.1rem

    .pb{$i}
        padding-bottom $gutter * $i * 0.1rem

    .pl{$i}
        padding-left $gutter * $i * 0.1rem

    .ma{$i}
        margin $gutter * $i * 0.1rem

    .mt{$i}
        margin-top $gutter * $i * 0.1rem

    .mr{$i}
        margin-right $gutter * $i * 0.1rem

    .mb{$i}
        margin-bottom $gutter * $i * 0.1rem

    .ml{$i}
        margin-left $gutter * $i * 0.1rem
